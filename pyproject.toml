[project]
name = "deposit-rescue-api"
version = "0.1.0"
description = "FastAPI backend for DepositRescue Vercel deployment"
requires-python = ">=3.9"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn>=0.30.0",
    "pydantic>=2.0.0",
    "groq>=0.11.0",
    "python-dotenv>=1.0.0",
]
